machine:
  kubelet:
    extraArgs:
      allowed-unsafe-sysctls: "net.ipv6.*"
    extraConfig:
      serializeImagePulls: false
      imageMaximumGCAge: 60m
    nodeIP:
      validSubnets:
        - 10.0.10.0/24
